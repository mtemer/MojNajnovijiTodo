[build]
builder = "RAILPACK"
buildCommand = "dotnet publish TodoList/TodoList.csproj -c Release -o out"

[deploy]
startCommand = "dotnet out/TodoList.dll"
restartPolicyType = "ON_FAILURE"

[variables]
RAILPACK_INSTALL_CMD = "dotnet restore TodoList/TodoList.csproj"